/**
 * MenuBar — Placeholder for potential native-style menu bar
 * The app uses Electron's native menu so this is minimal
 */

import React from 'react';

export const MenuBar: React.FC = () => {
    return null; // Using native Electron menu instead
};
